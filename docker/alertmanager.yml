route:
  receiver: "tingfeng-agent"
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 30s
  group_by: ["alertname"]

receivers:
  - name: "tingfeng-agent"
    webhook_configs:
      - url: "http://host.docker.internal:8081/api/v1/alerts/webhook"
        send_resolved: false
